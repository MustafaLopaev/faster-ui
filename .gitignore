# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env*

# Build & test artifacts
coverage
storybook-static
cypress/screenshots
cypress/videos
cypress/downloads
*.tsbuildinfo
*.tgz

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Quality automation (004): fixture installs and per-run captures are never committed.
# `visual/baselines/` IS committed — it is the reviewable evidence (FR-029).
test/consumers/*/node_modules
test/consumers/*/.next
test/consumers/*/dist
visual/current/
visual/diff/
visual/report.json

# api-extractor scratch (the committed record lives in etc/)
temp/

# The overall results report (report.yml / npm run report) — a per-run artifact
overall-report.html
report-meta.json
model-usage.json
