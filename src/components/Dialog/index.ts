export { Dialog } from './Dialog'

// Both a const object and the union type derived from it (declaration merging).
export { DialogSize } from './Dialog.types'

export type { DialogProps } from './Dialog.types'
