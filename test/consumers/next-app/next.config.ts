import type { NextConfig } from 'next'

// Deliberately bare. A fixture that needs configuration to consume the package
// is not testing the package — it is testing the configuration.
const config: NextConfig = {}

export default config
