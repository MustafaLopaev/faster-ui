{
  "name": "fui-consumer-next-app",
  "private": true,
  "version": "0.0.0",
  "description": "Consumer fixture: App Router server render → hydrate, through a real framework. The only check that exercises the RSC boundary, streaming SSR and stylesheet order together. NOT a workspace.",
  "scripts": {
    "build": "next build",
    "start": "next start -p 3100"
  },
  "dependencies": {
    "next": "16.3.1",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "typescript": "6.0.3"
  }
}
